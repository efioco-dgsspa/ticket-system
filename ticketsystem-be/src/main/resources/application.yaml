spring:
  application:
    name: Ticket-System

  datasource:
    url: jdbc:postgresql://localhost:5432/ticket_system
    username: postgres
    password: admin_postgres#!
    driver-class-name: org.postgresql.Driver

  jackson:
    deserialization:
      fail-on-unknown-properties: false
      accept-single-value-as-array: true
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always

  flyway:
    enabled: true
    locations: classpath:db/migration

  mail:
    host: smtp.gmail.com
    port: 587
    username: dgs.spa.test@gmail.com
    password: pcdh jgrn fscs qyem
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  refresh:
    expiration: 1d

server:
  port: 8080
