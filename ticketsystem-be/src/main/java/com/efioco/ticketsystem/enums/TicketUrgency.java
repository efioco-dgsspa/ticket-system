package com.efioco.ticketsystem.enums;

public enum TicketUrgency {
    BLOCKING,          // Non si può lavorare
    HIGH,              // Si può lavorare con molta difficoltà
    MEDIUM,            // Si può lavorare con qualche difficoltà
    LOW                // Si può lavorare normalmente
}
